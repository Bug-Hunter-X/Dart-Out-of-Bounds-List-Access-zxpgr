```dart
List<int> numbers = [1, 2, 3, 4, 5];

// This will throw an error because you're trying to access an index that's out of bounds.
int lastNumber = numbers[numbers.length];

print(lastNumber);
```